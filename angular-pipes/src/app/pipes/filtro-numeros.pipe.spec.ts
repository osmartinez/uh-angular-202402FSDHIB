import { FiltroNumerosPipe } from './filtro-numeros.pipe';

describe('FiltroNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
