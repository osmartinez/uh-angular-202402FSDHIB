import { WordEllipsisPipe } from './word-ellipsis.pipe';

describe('WordEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new WordEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
